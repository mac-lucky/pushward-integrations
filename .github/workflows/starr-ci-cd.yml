name: CI/CD Starr

on:
  push:
    branches: [main]
    tags: ['v*']
    paths: ['starr/**', 'shared/**', '.github/workflows/starr-ci-cd.yml']
  pull_request:
    branches: [main]
    paths: ['starr/**', 'shared/**', '.github/workflows/starr-ci-cd.yml']
  workflow_dispatch:

jobs:
  ci-cd:
    uses: mac-lucky/actions-shared-workflows/.github/workflows/go-cicd-reusable.yml@master
    permissions:
      contents: read
      packages: write
      security-events: write
    with:
      app_name: "pushward-starr"
      ghcr_image: "ghcr.io/mac-lucky/pushward-starr"
      dockerhub_image: "maclucky/pushward-starr"
      push_to_dockerhub: false
      push_on_main: true
      main_branch: "main"
      build_context: "."
      dockerfile_path: "./starr/Dockerfile"
      go_module_path: "./starr"
      enable_go_tests: true
      go_test_args: "-race -count=1 -v"
      enable_container_tests: false
      enable_kubernetes_tests: false
    secrets: inherit
