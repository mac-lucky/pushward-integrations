name: CI/CD Sonarr

on:
  push:
    branches: [main]
    tags: ['v*']
    paths: ['sonarr/**', 'shared/**', '.github/workflows/sonarr-ci-cd.yml']
  pull_request:
    branches: [main]
    paths: ['sonarr/**', 'shared/**', '.github/workflows/sonarr-ci-cd.yml']
  workflow_dispatch:

jobs:
  ci-cd:
    uses: mac-lucky/actions-shared-workflows/.github/workflows/go-cicd-reusable.yml@master
    permissions:
      contents: read
      packages: write
      security-events: write
    with:
      app_name: "pushward-sonarr"
      ghcr_image: "ghcr.io/mac-lucky/pushward-sonarr"
      dockerhub_image: "maclucky/pushward-sonarr"
      push_to_dockerhub: false
      push_on_main: true
      main_branch: "main"
      build_context: "."
      dockerfile_path: "./sonarr/Dockerfile"
      go_module_path: "./sonarr"
      enable_go_tests: true
      go_test_args: "-race -count=1 -v"
      enable_container_tests: false
      enable_kubernetes_tests: false
    secrets: inherit
