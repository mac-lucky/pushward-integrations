name: CI/CD SABnzbd

on:
  push:
    branches: [main]
    paths: ['sabnzbd/**', 'shared/**', '.github/workflows/sabnzbd-ci-cd.yml']
  pull_request:
    branches: [main]
    paths: ['sabnzbd/**', 'shared/**', '.github/workflows/sabnzbd-ci-cd.yml']
  workflow_dispatch:

permissions:
  contents: read
  packages: write
  security-events: write
  actions: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  ci-cd:
    uses: mac-lucky/actions-shared-workflows/.github/workflows/go-cicd-reusable.yml@master
    with:
      app_name: "pushward-sabnzbd"
      ghcr_image: "ghcr.io/mac-lucky/pushward-sabnzbd"
      dockerhub_image: "maclucky/pushward-sabnzbd"
      push_to_dockerhub: false
      main_branch: "main"
      # Talos deployments consume the :main dev channel between releases
      enable_main_dev_tag: true
      build_context: "."
      dockerfile_path: "./sabnzbd/Dockerfile"
      go_module_path: "./sabnzbd"
      enable_go_tests: true
      go_test_args: "-race -count=1 -v"
      # golangci-lint.yml runs govet and staticcheck on the same events
      code_analysis_tools: '["govulncheck", "gosec"]'
      enable_container_tests: false
      enable_kubernetes_tests: false
    secrets: inherit
