name: CI/CD Radarr

on:
  push:
    branches: [main]
    tags: ['v*']
    paths: ['radarr/**', 'shared/**', '.github/workflows/radarr-ci-cd.yml']
  pull_request:
    branches: [main]
    paths: ['radarr/**', 'shared/**', '.github/workflows/radarr-ci-cd.yml']
  workflow_dispatch:

jobs:
  ci-cd:
    uses: mac-lucky/actions-shared-workflows/.github/workflows/go-cicd-reusable.yml@master
    permissions:
      contents: read
      packages: write
      security-events: write
    with:
      app_name: "pushward-radarr"
      ghcr_image: "ghcr.io/mac-lucky/pushward-radarr"
      dockerhub_image: "maclucky/pushward-radarr"
      push_to_dockerhub: false
      push_on_main: true
      main_branch: "main"
      build_context: "."
      dockerfile_path: "./radarr/Dockerfile"
      go_module_path: "./radarr"
      enable_go_tests: true
      go_test_args: "-race -count=1 -v"
      enable_container_tests: false
      enable_kubernetes_tests: false
    secrets: inherit
